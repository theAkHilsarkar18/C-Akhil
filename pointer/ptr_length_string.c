#include <stdio.h>
int main()
{
    // wap to find length of string using ptr

    char name[10] = "Red&White";
    int *ptr = name[0];
    int count = 0;

    for(int i=0; name[i]!='\0'; i++)
    {
        
    }
}